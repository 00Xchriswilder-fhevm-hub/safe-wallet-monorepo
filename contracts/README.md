# Safe Wallet — on-chain contracts

| Package                                              | Description                                                                      |
| ---------------------------------------------------- | -------------------------------------------------------------------------------- |
| [`fhevm-multisig-helper/`](./fhevm-multisig-helper/) | **FHEVMMultiSigHelper** — ACL preflight for Safe confidential ERC-7984 transfers |

The web app references deployed helper addresses via `NEXT_PUBLIC_*_FHEVM_MULTISIG_HELPER` in `apps/web/.env`.
